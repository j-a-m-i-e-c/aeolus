// escape-room-fx — visual implementation behind ui/index.tsx
import { useEffect, useState } from "react";
import { commandLadder, commandVerdict, percent, rungProps, toggleProps, verdictProps } from "@aeolus/ui";
const C: any = { calm: "#4BB8FF", puzzle: "#B26BFF", tension: "#FF625C", victory: "#63DF8B" };
export default function RoomSystemsPanel({ model, actions }: {
    model: Record<string, any>;
    actions: Record<string, (...args: any[]) => void>;
}) { const scene = String(model.scene || "puzzle"), smoke = Boolean(model.smoke), audio = String(model.audio || "clockwork"), lightPct = Number(model.lightPct ?? 62), pending = Boolean(model.pending), transitioning = Boolean(model.transitioning), changed = Number(model.changedAt ?? 0), last = model.lastAction as any, color = C[scene] || C.puzzle; const [p, setP] = useState(0); useEffect(() => { const id = setInterval(() => setP(v => (v + 1) % 10000), 100); return () => clearInterval(id); }, []); const beam = .08 + lightPct / 180;
    // Haze is a physical state the room controller reports, so the control is a
    // toggle over observed truth and stays pressable either way.
    const hazeVisual = toggleProps(smoke, { pending: pending || transitioning });
    const verdict = commandVerdict(model.lastCommand), rungs = commandLadder(model.lastCommand);
 return <div style={{ padding: 13, minHeight: "100%", background: "#0C0A0E", color: "#F0EBF3" }}><div style={{ display: "flex", justifyContent: "space-between", gap: 10 }}><div><div style={{ fontSize: 17, fontWeight: 900 }}>ROOM SYSTEMS</div><div style={{ fontSize: 12, color: "#8F8393", marginTop: 3 }}>Lighting · haze · soundscape · game-event transitions</div></div><div style={{ textAlign: "right" }}><div style={{ fontSize: 12, fontWeight: 850, color }}>{transitioning ? "TRANSITIONING" : scene.toUpperCase()}</div><div style={{ fontSize: 11, color: "#84788A", marginTop: 2 }}>{percent(lightPct)} light · {audio}</div></div></div><div style={{ border: "1px solid #332D37", borderRadius: 11, overflow: "hidden", marginTop: 9 }}><svg width="100%" height="210" viewBox="0 0 430 190"><rect width="430" height="190" fill="#100D12"/><rect x="34" y="26" width="362" height="133" rx="7" fill="#151118" stroke="#3C3341"/><path d="M55 30 L150 155 H275 L170 30Z" fill={color} opacity={beam}/><path d="M375 30 L280 155 H155 L260 30Z" fill={color} opacity={beam * .75}/><rect x="90" y="119" width="250" height="40" fill="#171219" stroke="#4B404F"/>{smoke && Array.from({ length: 14 }).map((_, i) => <ellipse key={i} cx={45 + ((i * 33 + p * .7) % 345)} cy={77 + ((i * 21 + p * .25) % 65)} rx={18 + i % 3 * 5} ry={7 + i % 2 * 4} fill="#E5E1E8" opacity={.04 + i % 3 * .018}/>)}<g transform="translate(75 57)"><circle r="16" fill="#0D0B10" stroke={color}/><path d="M-5 -6 L7 0 L-5 6Z" fill={color}/><text x="0" y="31" textAnchor="middle" fill="#8C8190" fontSize="10">AUDIO</text></g><g transform="translate(355 57)"><rect x="-14" y="-10" width="28" height="20" rx="3" fill="#0D0B10" stroke={smoke ? "#D7D2DB" : "#655E69"}/><path d="M-8 -14 Q0 -25 8 -14" fill="none" stroke={smoke ? "#D7D2DB" : "#655E69"}/><text x="0" y="31" textAnchor="middle" fill="#8C8190" fontSize="10">HAZE</text></g><text x="215" y="144" textAnchor="middle" fill={color} fontSize="13" fontWeight="900">{scene.toUpperCase()} LOOK</text><text x="215" y="178" textAnchor="middle" fill="#817584" fontSize="10">soundscape: {audio} · lighting {percent(lightPct)}</text></svg></div><div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 7, marginTop: 8 }}>{[["LIGHTING", percent(lightPct), color], ["SOUNDSCAPE", audio.toUpperCase(), "#C99AE1"], ["HAZE", smoke ? "ACTIVE" : "CLEAR", smoke ? "#DFD9E3" : "#8B858F"]].map((m: any) => <div key={m[0]} style={{ border: "1px solid #39323D", borderRadius: 9, padding: 9, background: "#100D12" }}><div style={{ fontSize: 11, color: "#8F8494" }}>{m[0]}</div><div style={{ fontSize: 13, fontWeight: 850, color: m[2], marginTop: 3 }}>{m[1]}</div></div>)}</div><div style={{ marginTop: 8, border: "1px solid #39323D", borderRadius: 10, padding: 9, background: "#100D12" }}><div style={{ fontSize: 11, color: "#A095A5", letterSpacing: ".1em", marginBottom: 7 }}>OPERATOR CONTROLS</div><button {...hazeVisual} style={{ ...hazeVisual.style, width: "100%", padding: "9px" }} onClick={() => actions.toggleSmoke()}>{pending || transitioning ? "Verifying haze command…" : smoke ? "Clear atmospheric haze" : "Add atmospheric haze"}</button><div style={{ fontSize: 11, color: "#817785", marginTop: 7 }}>Room looks come from Game Master over Automation Events. This automation owns and verifies the physical room controller.</div></div><div style={{ fontSize: 11, color: "#7C7280", marginTop: 7 }}>{last?.label ? String(last.label) : changed ? "Room systems updated" : "Room systems online"}</div>    {/* What the last command actually proved. Every rung is a durable record the
        runtime wrote, not a step this pane inferred from the outcome. */}
    {verdict && <div style={{ marginTop: 8, border: "1px solid #39323D", borderRadius: 9, padding: 9, background: "#100D12" }}>
      <div style={{ display: "flex", justifyContent: "space-between", gap: 8, marginBottom: 5 }}><div style={{ fontSize: 11, color: "#8F8494", letterSpacing: ".1em" }}>COMMAND EVIDENCE</div><div style={verdictProps(verdict)}>{verdict.headline}</div></div>
      {rungs.map((rung: any) => { const rv = rungProps(rung); return <div key={rung.state} style={rv.style}><span>{rv.mark}</span><span>{rung.label}</span>{rung.detail && <span style={{ color: "#817785" }}>{rung.detail}</span>}</div>; })}
      <div style={{ fontSize: 11, color: "#817785", marginTop: 5 }}>{verdict.clampNote || verdict.detail}</div>
    </div>}
    </div>; }
