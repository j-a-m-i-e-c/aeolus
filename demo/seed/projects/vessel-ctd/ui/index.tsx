import { useEffect, useState } from "react";

function clamp(v:number,a:number,b:number){return Math.min(b,Math.max(a,v));}
export default function CtdOperations(aeolus: CustomComponentProps) {
  const depth = clamp(Number(aeolus.read("depth") ?? 120),0,500);
  const target = clamp(Number(aeolus.read("targetDepth") ?? 120),0,500);
  const status = String(aeolus.read("status") || "holding");
  const temp = Number(aeolus.read("temperature") ?? 12.1); const sal = Number(aeolus.read("salinity") ?? 35.1); const oxy = Number(aeolus.read("oxygen") ?? 5.8);
  const tension = Number(aeolus.read("tension") ?? 220); const speed = Number(aeolus.read("verticalSpeed") ?? 0); const pending = Boolean(aeolus.read("commandPending"));
  const last = aeolus.read("lastAction") as any; const [phase,setPhase]=useState(0);
  useEffect(()=>{const id=setInterval(()=>setPhase(v=>(v+1)%100000),90);return()=>clearInterval(id);},[]);
  const action=last?.label?String(last.label):"CTD telemetry online"; const tensionHigh=tension>=650; const moving=status==="deploying"||status==="recovering";
  const tempAt=(d:number)=>18.5-14.3/(1+Math.exp(-(d-90)/18)); const salAt=(d:number)=>35.0-.4/(1+Math.exp(-(d-90)/40));
  const points:number[]=[];for(let d=0;d<=Math.max(30,depth);d+=18)points.push(d);if(points[points.length-1]!==depth)points.push(depth);
  const y=(d:number)=>24+d/500*185; const tx=(t:number)=>210+(t-3)/16*105; const sx=(s:number)=>335+(s-34.55)/.55*92;
  const tPath=points.map((d,i)=>(i?"L":"M")+tx(tempAt(d)).toFixed(1)+","+y(d).toFixed(1)).join(" "); const sPath=points.map((d,i)=>(i?"L":"M")+sx(salAt(d)).toFixed(1)+","+y(d).toFixed(1)).join(" ");
  return <div style={{padding:11,minHeight:"100%",background:"linear-gradient(180deg,#09131A,#061018)",color:"#EDF2F4"}}>
    <div style={{display:"flex",justifyContent:"space-between",marginBottom:7}}><div><div style={{fontSize:12,fontWeight:900}}>CTD OPERATIONS</div><div style={{color:"#687C87",fontSize:11,marginTop:2}}>Winch control · water-column profile · cable-tension protection</div></div><div style={{textAlign:"right"}}><div style={{color:tensionHigh?"#EF8A67":moving?"#69D7EF":"#84DBA2",fontSize:11,fontWeight:850}}>{tensionHigh?"TENSION INTERLOCK":status.toUpperCase()}</div><div style={{color:"#60737D",fontSize:11}}>{Math.round(depth)} m · target {Math.round(target)} m</div></div></div>
    <div style={{border:"1px solid #1F3948",borderRadius:10,background:"#05131C",overflow:"hidden"}}><svg width="100%" height="220" viewBox="0 0 500 220">
      <rect width="190" height="220" fill="#08283E"/><rect y="42" width="190" height="178" fill="#061A2A"/><path d="M0 42 Q45 36 95 42 T190 42" fill="none" stroke="#62BDD7" opacity=".45"/>
      {[0,100,200,300,400,500].map(d=><g key={d}><line x1="0" y1={y(d)} x2="190" y2={y(d)} stroke="#315369" opacity=".23"/><text x="5" y={y(d)-2} fill="#527489" fontSize="10">{d}</text></g>)}
      <line x1="92" y1="31" x2="92" y2={y(depth)} stroke="#A7B7BC"/><g transform={"translate(92 "+y(depth)+")"}><circle r="9" fill="#E5A64B" stroke="#FFD07B"/><rect x="-6" y="-12" width="12" height="3" fill="#C4CED0"/></g>
      {moving&&Array.from({length:4}).map((_,i)=><circle key={i} cx="92" cy={50+((phase*2+i*38)%Math.max(30,y(depth)-50))} r="1.5" fill="#70DBF3"/>)}
      <text x="108" y={y(depth)+3} fill="#F2C06F" fontSize="10">{Math.round(depth)}m</text>
      <rect x="190" width="310" height="220" fill="#071018"/><text x="210" y="15" fill="#6B818D" fontSize="10" letterSpacing="1">LIVE PROFILE</text>
      {[0,100,200,300,400,500].map(d=><line key={d} x1="210" y1={y(d)} x2="474" y2={y(d)} stroke="#1A2F3B"/>)}<path d={tPath} fill="none" stroke="#F0A84A" strokeWidth="2"/><path d={sPath} fill="none" stroke="#51D1EA" strokeWidth="1.7" strokeDasharray="4 3"/>
      <line x1="210" y1={y(depth)} x2="474" y2={y(depth)} stroke="#6ED899" strokeDasharray="3 3"/><text x="212" y="208" fill="#F0A84A" fontSize="10">TEMP {temp.toFixed(1)}°C</text><text x="302" y="208" fill="#51D1EA" fontSize="10">SAL {sal.toFixed(2)}</text><text x="404" y="208" fill="#83D29D" fontSize="10">O₂ {oxy.toFixed(1)}</text>
    </svg></div>
    <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:6,marginTop:7}}><div style={{border:"1px solid "+(tensionHigh?"#75412F":"#263C47"),borderRadius:8,padding:7,background:"#081117"}}><div style={{color:"#687D87",fontSize:11}}>CABLE TENSION</div><div style={{color:tensionHigh?"#F08B69":"#C8D5D9",fontFamily:"monospace",fontSize:14,fontWeight:800,marginTop:2}}>{Math.round(tension)} N</div><div style={{height:5,background:"#17262E",borderRadius:4,overflow:"hidden",marginTop:4}}><div style={{height:"100%",width:Math.min(100,tension/8)+"%",background:tensionHigh?"#D76645":"#4EACC1"}}/></div></div><div style={{border:"1px solid #263C47",borderRadius:8,padding:7,background:"#081117"}}><div style={{color:"#687D87",fontSize:11}}>VERTICAL SPEED</div><div style={{color:"#B9C9CF",fontFamily:"monospace",fontSize:14,fontWeight:800,marginTop:2}}>{speed.toFixed(1)} m/s</div><div style={{color:"#596E78",fontSize:11,marginTop:5}}>Profile values are physical sonde MQTT state.</div></div></div>
    <div style={{marginTop:7,border:"1px solid #263E49",borderRadius:9,padding:8,background:"#07141C"}}><div style={{color:"#80939C",fontSize:11,letterSpacing:".12em",marginBottom:6}}>OPERATOR CONTROLS</div><div style={{display:"flex",gap:5}}><button disabled={pending||moving} onClick={()=>aeolus.fire("deploy-420")} style={{flex:1,padding:"7px",borderRadius:6,border:"1px solid #285C70",background:"#0A2633",color:"#71D7EF",fontSize:11,cursor:"pointer"}}>Deploy 420 m</button><button disabled={pending} onClick={()=>aeolus.fire("hold-ctd")} style={{padding:"7px 9px",borderRadius:6,border:"1px solid #434A4E",background:"#161A1C",color:"#A9B3B7",fontSize:11,cursor:"pointer"}}>Hold</button><button disabled={pending||moving} onClick={()=>aeolus.fire("recover-ctd")} style={{padding:"7px 9px",borderRadius:6,border:"1px solid #375945",background:"#102219",color:"#87D9A0",fontSize:11,cursor:"pointer"}}>Recover</button></div></div>
    <div style={{marginTop:7,border:"1px dashed #69502E",borderRadius:9,padding:8,background:"#171309"}}><div style={{color:"#D8B66D",fontSize:11,letterSpacing:".12em"}}>DEMO SCENARIO</div><div style={{color:"#806F50",fontSize:11,margin:"3px 0 6px"}}>Inject a physical cable problem. The CTD automation should arrest the winch itself.</div><div style={{display:"flex",gap:5}}><button onClick={()=>aeolus.fire("simulate-snag")} style={{flex:1,padding:"6px",borderRadius:6,border:"1px solid #6A5130",background:"#21180B",color:"#E3B866",fontSize:11,cursor:"pointer"}}>Inject cable snag</button><button onClick={()=>aeolus.fire("reset-ctd")} style={{padding:"6px 9px",borderRadius:6,border:"1px solid #454138",background:"#171713",color:"#898B82",fontSize:11,cursor:"pointer"}}>Reset cast</button></div></div>
    <div style={{color:"#5B6E77",fontSize:11,marginTop:6,whiteSpace:"nowrap",overflow:"hidden",textOverflow:"ellipsis"}}>{action}</div>
  </div>;
}