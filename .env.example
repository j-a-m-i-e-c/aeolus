# Aeolus backend configuration

NODE_ENV=development
PORT=3001
LOG_LEVEL=debug

# MQTT
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_TOPICS="#"

# Database and history
DB_PATH=./data/aeolus.db
STATE_HISTORY_MAX=100
HISTORY_RECORD_INTERVAL=5000

# HTTP security and limits
RATE_LIMIT_RPM=1000
# CORS_ORIGINS=https://aeolus.local
# JWT_SECRET=replace-with-a-long-random-value

# Prometheus scrape authentication. Leave unset for local-only open metrics.
# METRICS_TOKEN=replace-with-a-random-value

# MQTT provisioning integration. These are only useful when the deployment
# gives the backend scoped access to the Mosquitto files and reload mechanism.
# AEOLUS_PROJECT_DIR=/path/to/aeolus
# MQTT_PASSWORD_FILE=/path/to/mosquitto/password_file

# Docker Compose host ports
API_PORT=3001
FRONTEND_PORT=3000
MQTT_PORT=1883
