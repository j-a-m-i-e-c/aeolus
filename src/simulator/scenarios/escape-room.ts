import type {AnyDeviceDefinition,DeviceModelFactoryContext,SimulatedInboundCommand,SimulatedCommandOutcome,SimulatedState,SimulatedStateController,SimulatorScenario} from "../types.js";
export const ESCAPE_ROOM_SCENARIO_KEY="escape-room";export const ESCAPE_ROOM_DEVICE_KEYS={puzzles:"escape-puzzles",exit:"escape-exit",hint:"escape-hint",fx:"escape-fx"} as const;export const ESCAPE_ROOM_STATE_TOPICS={puzzles:"sensor/escape/puzzles",exit:"switch/escape/exit/state",hint:"switch/escape/hint-screen/state",fx:"switch/escape/fx/state"} as const;export const ESCAPE_ROOM_COMMAND_TOPICS={exit:"switch/escape/exit/set",hint:"switch/escape/hint-screen/set",fx:"switch/escape/fx/set"} as const;export const ESCAPE_ROOM_STIMULUS={solveNext:"escape/sim/solve-next",reset:"escape/sim/reset"} as const;
const INITIAL={puzzles:{p1:true,p2:false,p3:false,attempts:[3,2,1],lastSolved:1},exit:{locked:true},hint:{on:true,hintsSent:1,message:"Look closer at the bookshelf."},fx:{scene:"puzzle",smoke:false}};
class Env{private c=new Map<string,SimulatedStateController>();register(k:string,s:SimulatedStateController){this.c.set(k,s)}get(k:string){return this.c.get(k)}reset(){this.get(ESCAPE_ROOM_DEVICE_KEYS.puzzles)?.update({...INITIAL.puzzles},{forcePublish:true});this.get(ESCAPE_ROOM_DEVICE_KEYS.exit)?.update({...INITIAL.exit},{forcePublish:true});this.get(ESCAPE_ROOM_DEVICE_KEYS.hint)?.update({...INITIAL.hint},{forcePublish:true});this.get(ESCAPE_ROOM_DEVICE_KEYS.fx)?.update({...INITIAL.fx},{forcePublish:true})}solveNext(){const p=this.get(ESCAPE_ROOM_DEVICE_KEYS.puzzles);if(!p)return;const s=p.read();if(!s.p1)p.update({p1:true,lastSolved:1},{forcePublish:true});else if(!s.p2)p.update({p2:true,lastSolved:2},{forcePublish:true});else if(!s.p3)p.update({p3:true,lastSolved:3},{forcePublish:true})}}
function sensor(k:string,n:string,t:string,i:SimulatedState,e:Env):AnyDeviceDefinition{return{key:k,name:n,stateTopic:t,initialState:i,createModel:(c)=>{e.register(c.key,c.state);return{getState:()=>c.state.read()}}}}function actuator(k:string,n:string,st:string,ct:string,i:SimulatedState,e:Env,h:(ctx:DeviceModelFactoryContext,c:SimulatedInboundCommand)=>SimulatedCommandOutcome):AnyDeviceDefinition{return{key:k,name:n,stateTopic:st,commandTopic:ct,initialState:i,commandProfile:{acknowledgement:{supported:true},qos:1},createModel:(c)=>{e.register(c.key,c.state);return{getState:()=>c.state.read(),onCommand:(x)=>h(c,x)}}}}
export function createEscapeRoomScenario():SimulatorScenario{const e=new Env();const exit=actuator(ESCAPE_ROOM_DEVICE_KEYS.exit,"Exit Maglock",ESCAPE_ROOM_STATE_TOPICS.exit,ESCAPE_ROOM_COMMAND_TOPICS.exit,{...INITIAL.exit},e,(ctx,c)=>{if(typeof c.params.locked!=="boolean")return{accepted:false,error:"maglock requires boolean locked"};return{accepted:true,state:{patch:{locked:c.params.locked}}}});const hint=actuator(ESCAPE_ROOM_DEVICE_KEYS.hint,"Hint Screen",ESCAPE_ROOM_STATE_TOPICS.hint,ESCAPE_ROOM_COMMAND_TOPICS.hint,{...INITIAL.hint},e,(ctx,c)=>{const message=String(c.params.message||"");if(!message)return{accepted:false,error:"hint screen requires message"};return{accepted:true,state:{patch:{on:true,message,hintsSent:Number(ctx.state.read().hintsSent??0)+1}}}});const fx=actuator(ESCAPE_ROOM_DEVICE_KEYS.fx,"Room FX Controller",ESCAPE_ROOM_STATE_TOPICS.fx,ESCAPE_ROOM_COMMAND_TOPICS.fx,{...INITIAL.fx},e,(_ctx,c)=>{const scene=String(c.params.scene||"puzzle"),smoke=Boolean(c.params.smoke);if(!["calm","puzzle","tension","victory"].includes(scene))return{accepted:false,error:"unknown room scene"};return{accepted:true,state:{patch:{scene,smoke}}}});return{key:ESCAPE_ROOM_SCENARIO_KEY,devices:[sensor(ESCAPE_ROOM_DEVICE_KEYS.puzzles,"Puzzle Sensor Network",ESCAPE_ROOM_STATE_TOPICS.puzzles,{...INITIAL.puzzles},e),exit,hint,fx],stimuli:{[ESCAPE_ROOM_STIMULUS.solveNext]:()=>e.solveNext(),[ESCAPE_ROOM_STIMULUS.reset]:()=>e.reset()}}}
