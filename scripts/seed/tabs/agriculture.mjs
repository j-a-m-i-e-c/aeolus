// Compatibility wrapper: Agriculture is implemented as a small module tree so
// each domain automation can evolve independently without mangling one huge file.
export { default } from "./agriculture/index.mjs";
