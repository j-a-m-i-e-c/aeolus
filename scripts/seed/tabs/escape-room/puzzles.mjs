const logic=`automation({actions:[function puzzleProgress(context){var topic=String(context.topic||"");var evt=topic.split("/").pop();function d(){return devices.list().find(function(x){return x.topic==="sensor/escape/puzzles";});}function action(l){state.set("lastAction",{label:l,at:Date.now()});}function project(){var x=d(),s=x&&x.state?x.state:{};var p1=Boolean(s.p1),p2=Boolean(s.p2),p3=Boolean(s.p3),solved=(p1?1:0)+(p2?1:0)+(p3?1:0);state.set("p1",p1);state.set("p2",p2);state.set("p3",p3);state.set("solved",solved);state.set("lastSolved",Number(s.lastSolved||0));if(Number(state.get("previousSolved")||0)!==solved){state.set("previousSolved",solved);action(solved===3?"Final puzzle physically solved":"Puzzle "+Number(s.lastSolved||solved)+" solved by team");events.emit("escape/puzzles/status",{p1:p1,p2:p2,p3:p3,solved:solved,complete:solved===3,lastSolved:Number(s.lastSolved||0)});}}
if(topic.indexOf("ui/")===0){if(evt==="simulate-solve"){events.emit("escape/sim/solve-next",{});action("Injecting the next participant puzzle solve");}else if(evt==="reset-puzzles"){events.emit("escape/sim/reset",{});state.set("previousSolved",-1);action("Resetting physical puzzle network");}return;}if(topic!=="sensor/escape/puzzles")return;project();}]});`;
const ui=`import type {CustomComponentProps} from "./types";export default function Puzzles(aeolus:CustomComponentProps){const p=[Boolean(aeolus.read("p1")??true),Boolean(aeolus.read("p2")),Boolean(aeolus.read("p3"))],solved=Number(aeolus.read("solved")??1),last=aeolus.read("lastAction") as any;return <div style={{padding:11,minHeight:"100%",background:"#0D0B10",color:"#EFEAF3"}}><div style={{display:"flex",justifyContent:"space-between"}}><div><div style={{fontSize:12,fontWeight:900}}>PUZZLE PROGRESS</div><div style={{fontSize:7.5,color:"#796F7E",marginTop:2}}>Physical puzzle sensors → progress domain event</div></div><div style={{fontSize:9,fontWeight:850,color:solved===3?"#73DA94":"#B87AEB"}}>{solved}/3 SOLVED</div></div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:7,marginTop:9}}>{[["CIPHER BOOKCASE","Rotary code"],["LASER GRID","Beam sequence"],["BALANCE SCALE","Brass weights"]].map((m:any,i)=><div key={m[0]} style={{border:"1px solid "+(p[i]?"#356047":"#3B3340"),borderRadius:9,padding:9,background:p[i]?"#0F1B14":"#121016"}}><div style={{width:24,height:24,borderRadius:99,display:"grid",placeItems:"center",background:p[i]?"#183C26":"#211A26",color:p[i]?"#79E09A":"#A694AF",fontSize:11,fontWeight:900}}>{p[i]?"✓":i+1}</div><div style={{fontSize:8,fontWeight:800,color:p[i]?"#9AD7AA":"#C1B3C6",marginTop:7}}>{m[0]}</div><div style={{fontSize:6.7,color:"#716876",marginTop:2}}>{m[1]}</div></div>)}</div><div style={{marginTop:8,border:"1px dashed #685337",borderRadius:9,padding:8,background:"#18130A"}}><div style={{fontSize:6.5,color:"#D9B973",letterSpacing:".12em"}}>DEMO SCENARIO</div><div style={{fontSize:7,color:"#887655",margin:"3px 0 6px"}}>Pretend the players physically solve the next puzzle. Aeolus only sees the resulting sensor state.</div><div style={{display:"flex",gap:5}}><button disabled={solved===3} onClick={()=>aeolus.fire("simulate-solve")} style={{flex:1,padding:"7px",borderRadius:6,border:"1px solid #62467A",background:"#1C1025",color:"#C596E5",fontSize:7.5,cursor:"pointer"}}>Players solve next puzzle</button><button onClick={()=>aeolus.fire("reset-puzzles")} style={{padding:"7px 10px",borderRadius:6,border:"1px solid #4A463E",background:"#171613",color:"#928D87",fontSize:7.5,cursor:"pointer"}}>Reset room</button></div></div><div style={{fontSize:7,color:"#66606A",marginTop:6}}>{last?.label?String(last.label):"Puzzle sensor network online"}</div></div>}`;
export const puzzleProgressAutomation={key:"escape-puzzles",name:"Puzzle Progress",triggerTopic:"sensor/escape/puzzles",scriptSource:logic,uiSource:ui,demoAccess:{fireEvents:["simulate-solve","reset-puzzles"]}};
