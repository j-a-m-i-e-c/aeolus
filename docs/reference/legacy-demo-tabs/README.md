# Historical demo-tab source snapshots

The files in this directory are retained for historical/reference context only. They predate the current Automation Project seed layout and are **not** the source loaded by the public demo or examples to copy for new authoring.

Current seeded Automation Project source lives under:

```text
scripts/seed/projects/
```

Current demo metadata/manifests live under:

```text
scripts/seed/tabs/
```

For the supported authoring model, see `docs/architecture/AUTOMATION_PROJECTS.md` and `docs/reference/automations.md`.
